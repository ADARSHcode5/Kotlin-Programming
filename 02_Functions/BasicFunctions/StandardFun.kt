/**
 * Standard function that takes two explicit integers and returns an integer.
 * Good practice: Document complex logic using KDoc comments (like this one).
 */
fun addNumbers(a: Int, b: Int): Int {
    // The 'return' keyword is mandatory in standard block body functions
    return a + b
}